---
name: modular
description: Design or refactor a codebase into clean, low-coupling modules so any single change only needs a small slice of the system loaded into context. Two modes — refactor an existing/legacy project, or architect a brand-new one to start modular. Use whenever the user wants to modularize, reduce coupling/dependencies, split a large file, untangle tangled code, review module boundaries, or plan a new project's architecture — even if they don't say "modularize".
---

# modular — PLACEHOLDER

> ⚠️ This is a placeholder. Replace the contents of this folder with the real
> `code-modularization` skill files (the router `SKILL.md` + the per-mode workflow files).
>
> When you do, **rename the frontmatter** `name: code-modularization` → `name: modular`
> to match the new short handle (`vibe:modular`).
