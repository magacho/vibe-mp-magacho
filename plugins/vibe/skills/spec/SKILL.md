---
name: spec
description: Refine, detail and pressure-test a demand (user story, feature request, or requirement) BEFORE any technical breakdown — surfacing shadow areas and missing information and deciding whether it is ready to build. Use whenever the user wants to detail, refine or "spec" a demand, write acceptance criteria, hunt for edge cases or open questions, generate Gherkin scenarios, or check if a story is ready — even if they don't say "spec".
---

# spec — PLACEHOLDER

> ⚠️ This is a placeholder. Replace the contents of this folder with the real
> `requirement-refiner` skill files (the `SKILL.md` + its `references/`).
>
> When you do, **rename the frontmatter** `name: requirement-refiner` → `name: spec`
> to match the new short handle (`vibe:spec`). Keep the `description` (it's what
> triggers the skill); just confirm it still reads well.
